name: markdown_text
description: "wippy designsystem component markdown_text"
version: 0.0.1

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  foundation:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
